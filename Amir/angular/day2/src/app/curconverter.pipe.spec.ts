import { CurconverterPipe } from './curconverter.pipe';

describe('CurconverterPipe', () => {
  it('create an instance', () => {
    const pipe = new CurconverterPipe();
    expect(pipe).toBeTruthy();
  });
});

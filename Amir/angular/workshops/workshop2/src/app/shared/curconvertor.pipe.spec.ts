import { CurconvertorPipe } from './curconvertor.pipe';

describe('CurconvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new CurconvertorPipe();
    expect(pipe).toBeTruthy();
  });
});

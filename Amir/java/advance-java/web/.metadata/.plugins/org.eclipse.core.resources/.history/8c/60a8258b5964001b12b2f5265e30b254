package com.cybage;

import java.io.IOException;
import java.io.PrintWriter;

import javax.servlet.RequestDispatcher;
import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

/**
 * Servlet implementation class First
 */
public class First extends HttpServlet {
	private static final long serialVersionUID = 1L;

	/**
	 * @see HttpServlet#HttpServlet()
	 */
	public First() {
		super();

	}

	protected void doGet(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {

		PrintWriter writer = response.getWriter();
		writer.write("<h1> This is get method (First Servlet)");
		
		// Reading data sent by users
		
		
		
		
		
		
		
		
		
		
		
		
		// After getting this servlet i want to go to hello servlet
		// RequestDispatcher rd = request.getRequestDispatcher("hello");
		// rd.forward(request, response); // going to other resources like (servlet and
		// jsp)
		// if want to go outside application
		// response.sendRedirect("https://www.google.com");
	}

	protected void doPost(HttpServletRequest request, HttpServletResponse response)
			throws ServletException, IOException {
		PrintWriter writer = response.getWriter();
		writer.write("<h1> This is post method (First Servlet)");
		
		
		request.getParameter("")l;

	}
}

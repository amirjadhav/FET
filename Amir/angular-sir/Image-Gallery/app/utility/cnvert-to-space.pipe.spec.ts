import { CnvertToSpacePipe } from './cnvert-to-space.pipe';

describe('CnvertToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new CnvertToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
